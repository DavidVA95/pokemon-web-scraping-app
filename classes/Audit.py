class Audit:

    def __init__(self, web_page, registers_number, errors, status):
        self.web_page = web_page
        self.registers_number = registers_number
        self.errors = errors
        self.status = status
